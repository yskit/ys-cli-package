const Controller = require('ys-class');

module.exports = class <%= name.charAt(0).toUpperCase() + name.substring(1) %>Controller extends Controller {
  constructor(...args) {
    super(...args);
  }

  common() {
    this.ctx.body = 'ok';
  }
};